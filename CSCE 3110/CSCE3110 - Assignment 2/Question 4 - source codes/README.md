# Compile and run balancing.cpp
g++ balancing.cpp -o balance
./balance

# Compile and run infix2postfix.cpp
g++ infix2postfix.cpp -o infix
./infix

# Compile and run postfixEval.cpp
g++ postfixEval.cpp -o eval
./eval